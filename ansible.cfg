#ANSIBLE CONFIGURATION FILE

[defaults]

inventory = ./inventories

roles_path = ./roles

remote_user = expatpirate

host_key_checking = False

retry_files_enabled = False

forks = 10

stdout_callbak = yaml

interpreter_python = auto

[privilege_escalation]

become = True

become_method = sudo

become_user = root

become_ask_pass = False
